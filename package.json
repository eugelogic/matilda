{
  "private": true,
  "scripts": {
    "build": "node bin/cmd.js",
    "clean": "rimraf ./public"
  },
  "dependencies": {
    "chalk": "^4.1.0",
    "fs-extra": "^9.0.1",
    "recursive-readdir": "^2.2.2",
    "rimraf": "^3.0.2"
  }
}
